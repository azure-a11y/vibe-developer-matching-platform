import { ComingSoon } from '@/components/ComingSoon';

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="pluug 문의 폼 링크, 브랜드명 등 사이트 전역 설정을 관리하는 화면입니다. SiteSettings(key-value) 저장소가 먼저 필요합니다."
      reference="docs/project/06_데이터모델.md §3.7"
    />
  );
}
