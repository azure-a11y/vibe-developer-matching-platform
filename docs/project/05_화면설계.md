# 05. 화면설계 — 보류

> 작성일 2026-08-14 · 상태: **차단됨(blocked)** — 확정본 작성 불가
> 소스가 될 자료: `artifact/builder-group`, `artifact/admin` (요구사항 확인용 레퍼런스, 코드 아님)

## 왜 지금 확정할 수 없는가

`02_화면목록(IA).md`와 동일한 이유 — **Q18(1안/2안 디자인 통합) 미해결**. 화면설계는 확정된 디자인
시스템 위에서 나와야 한다. `artifact/builder-group`·`artifact/admin`은 "요구사항이 확인된
레퍼런스"일 뿐 그대로 화면설계로 승격하지 않는다(`CLAUDE.md` "기존 디자인 산출물 취급" 원칙,
`wiki/09-phase-design.md`).

상세 컴포넌트의 "AI스러운 느낌"(Q19)도 구체적 판단 기준이 아직 없다 — 무엇을 고쳐야 "화면설계
확정"으로 볼 수 있는지 자체가 정의되지 않은 상태다.

## 지금 할 수 있는 선행 작업 (화면설계 자체는 아님)

`docs/planning/AI빌더그룹_프로젝트전환계획.md` §8 순서 4번 — `artifact/builder-group` 상세 컴포넌트를
`CLAUDE.md` "피해야 할 전형적인 AI 디자인" 체크리스트 기준으로 자체 감사(audit)해 Q19의 판단
기준을 마련하는 것은 지금 가능하다. 단, 이 문서(05) 자체의 확정본 작성 선행조건은 아니며 별도
작업으로 취급한다.

## 재개 조건

1. Q18 확정 → 하나의 디자인 시스템으로 통합된 정적 산출물 완성(전환계획 §7 Phase 1)
2. 팀장 2인 승인(투표 금지)
3. 확정 디자인 기준으로 화면설계 재작성

## 관련 문서

- `docs/planning/AI빌더그룹_프로젝트전환계획.md` §7 Phase 1 — Exit 기준·병렬 금지 원칙
- `02_화면목록(IA).md`
